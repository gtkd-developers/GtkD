name = cairo_clock
version = 0.1

requires = gtkd 

[main.d]
type = binary
target = cairo_clock
buildflags = -I../../demos/ 
version(linux) {
    buildflags = +=-I/usr/local/include/d -L-ldl
}
version(Windows){
    buildflags+=-lladvapi32
}

