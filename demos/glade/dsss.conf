name = gladeTest

requires = gtkd

[gladeTest.d]
type = binary
target = gladeTest
buildflags = -I../ -I../../src -no-export-dynamic
version(linux) {
buildflags +=-L-ldl
}
version(Windows){
    buildflags+=-lladvapi32
}

